name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
    paths:
      - "client/**"
      - ".github/workflows/deploy.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"
          cache: "pnpm"
          cache-dependency-path: "client/pnpm-lock.yaml"

      - name: Install dependencies
        run: cd client && pnpm install

      - name: Build
        run: cd client && pnpm run build
        env:
          # Замените на ваш IP адрес или используйте GitHub Secrets
          VITE_WS_URL: ${{ secrets.VITE_WS_URL || 'ws://localhost:3000/ws' }}
          GITHUB_PAGES: true
          # Замените 'wsock' на имя вашего репозитория, если оно отличается
          VITE_BASE_PATH: ${{ secrets.VITE_BASE_PATH || '/wsock/' }}

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: "./client/dist"

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
